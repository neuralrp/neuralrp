[pytest]
testpaths = tests
addopts = -ra -p no:cacheprovider -m "not legacy and not e2e" --ignore=tests/legacy
markers =
    legacy: Tests for deprecated or removed behaviors kept for reference.
    e2e: End-to-end tests that require external services or browser automation.
